# BuckDuit Flask + React — Render Config (Final Fixed)

services:
  - type: web
    name: buckduit-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
    plan: free
    healthCheckPath: /health
    autoDeploy: true

  - type: static
    name: buckduit-frontend
    buildCommand: cd buckduit-frontend && npm install && npm run build
    publishPath: buckduit-frontend/build
    plan: free
    autoDeploy: true
